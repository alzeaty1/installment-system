# بلان: نسخة من مشروع التقسيط على D + رفع نسخة على GitHub
التاريخ: 2026-09-14
المصدر: `C:/Users/Elnour Tech/installment-system` (فرع `codex-updates`، ‏~194M)
الريموت الحالي: `https://github.com/alzeaty1/installment-system.git`

## ملاحظة مهمة (سبب التركيز)
- `D:/installments` موجود أصلاً وفيه نسخة قديمة (فيه `manage.py` و`core`)، و`D:/installment-system-test` موجود برضه.
- عشان كده **أهم خطوة هي T1**: اختيار اسم مجلد جديد غير متصادم + استبعاد الأسرار وملفات البيئة من النسخة، قبل أي نسخ.

## التاسكات (كل واحدة 2-5 دقائق)

### T1 — تجهيز الوجهة والاستبعادات (أهم خطوة)
- الاتفاق على اسم المجلد الجديد (المقترح: `D:/installment-system-copy`).
- فحص المصدر: ملفات `.env` / أسرار / `venv/` / `__pycache__` / `db.sqlite3` — تُستبعد من النسخة (تتنسخ نظيفة).
- التحقق من مساحة D (المتاح 43G — كافية).

### T2 — النسخ الفعلي لـ D
- نسخ شجرة العمل (نفس حالة فرع `codex-updates` الحالية، غير الملتزم فيها `.agents/` و`AGENTS.md` وغيرها — يُتفق هل تُنسخ أم لا).
- التحقق بعد النسخ: مقارنة عدد الملفات + `manage.py check` في النسخة.

### T3 — تجهيز الريبو الجديد على GitHub
- الاتفاق على اسم الريبو الجديد (المقترح: `installment-system-copy`) + عام/خاص.
- إنشاء الريبو (من حساب `alzeaty1`) وربطه بالنسخة كـ `origin` جديد (النسخة الجديدة فقط — الأصل يفضل كما هو).

### T4 — أول Push للنسخة + عرض النتيجة
- `commit` أول + `push` للريبو الجديد.
- عرض جدول قصير: المجلد + رابط الريبو + كيف يجربها.

## أسئلة محتاجة ردك قبل التنفيذ
1. اسم مجلد الوجهة على D؟ (المقترح: `D:/installment-system-copy`)
2. اسم الريبو الجديد على GitHub؟ عام ولا خاص؟ (المقترح: `installment-system-copy` خاص)
3. النسخة تشمل الملفات غير الملتزَمة (`.agents/` و`AGENTS.md`...) ولا نسخة نظيفة من آخر commit فقط؟
4. ننسخ قاعدة البيانات `db.sqlite3` (لو موجودة) ولا نسخة نظيفة بدون بيانات؟

## قاعدة الفلو
- بعد موافقتك: أنفذ **T1 فقط** → اختبار → عرض → انتظار "تمام"، ثم اللي بعده. تاسك واحد في المرة.
